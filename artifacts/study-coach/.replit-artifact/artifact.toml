kind = "web"
previewPath = "/"
title = "Study Coach"
version = "1.0.0"
id = "artifacts/study-coach"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22819

[services.development]
run = "pnpm --filter @workspace/study-coach run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/study-coach", "run", "build" ]
publicDir = "artifacts/study-coach/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22819"
BASE_PATH = "/"
