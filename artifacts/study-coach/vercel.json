{
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "https://next-topic-api-YOURNAME.koyeb.app/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}

